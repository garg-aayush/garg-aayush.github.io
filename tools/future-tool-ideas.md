# Future Tool Ideas

Ideas for additional browser-based tools to add to the tools section.

- **CSV/TSV Viewer** — sortable, filterable table with column stats
- **Regex Tester** — live regex matching with capture groups
- **Token Counter & Visualizer** — tiktoken-based tokenization display
- **Color Picker/Palette Generator** — HSL/RGB/hex with palette export
